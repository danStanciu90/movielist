import { createTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#34495e' },
  secondary: { main: '#f1c40f', contrastText: '#ffffff' },
};

export const theme = createTheme({ palette });
